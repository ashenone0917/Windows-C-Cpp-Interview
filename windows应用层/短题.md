#### C++ 赋值构造，拷贝构造:
```cpp
Object& operator=(Object&);
Object& operator=(const Object&);
Object(Object&);
Object(const Object&);
```

#### Windows内核对象、句柄：
Process thread event等内核对象句柄：
- 句柄值本进程内可使用，为句柄表索引
- 索引指向的对象全局存在

窗口句柄：
- 句柄值本session全局皆可使用
- 不可跨session

#### C++对象内存结构分布


